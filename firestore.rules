rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Live artifacts: students post, everyone reads, anyone can delete (honor code + admin URL gates UI).
    match /artifacts/{artifactId} {
      allow read: if true;
      allow create: if request.resource.data.keys().hasAll(['benchmarkId','studentName','description','createdAt']) &&
                       request.resource.data.studentName is string &&
                       request.resource.data.studentName.size() <= 80 &&
                       request.resource.data.description.size() <= 500;
      allow delete: if true;
      allow update: if false;
    }

    // Archived artifacts: same model, separated for class history.
    match /archives/{archiveId}/artifacts/{artifactId} {
      allow read: if true;
      allow create, delete: if true;
      allow update: if false;
    }
  }
}
